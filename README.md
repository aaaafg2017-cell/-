# Notes App

[العربية](README.ar.md) · [API](docs/api.md) · [Architecture](docs/architecture.md)

A small full-stack notes app used to bootstrap this repository. It has two
workspaces:

- **`client/`** — React + Vite + TypeScript single-page app.
- **`server/`** — Express + TypeScript REST API with file-backed notes.

The client proxies `/api/*` requests to the server during development, so you
run both together and interact with a single URL.

## Features

- Create, edit, and delete notes (title + optional body).
- Search the visible list, including Arabic spelling variants (alef, tashkeel,
  hamza, Persian yeh/kaf, Eastern Arabic digits) and Latin accents.
- Export the matching notes as JSON or Markdown from the UI, or all notes from
  the API.
- Browser-language UI: Arabic + RTL when the browser prefers `ar`, otherwise
  English + LTR.
- File persistence that survives API restarts, with atomic writes and
  fail-closed behavior if the data file is corrupt.
- Unsaved-change warnings, delete confirmation, and retry on load failure.

## Prerequisites

- Node.js `>= 20` (developed against Node 22)
- npm `>= 10`

## Getting started

```bash
npm install        # install all workspace dependencies
npm run dev        # start the API (:3001) and the client (:5173) together
```

Then open http://localhost:5173 and create a note.

For a production build, the API also serves the compiled client:

```bash
npm run build
npm start          # http://localhost:3001 (API + UI)
```

## Common commands

| Command | Description |
| --- | --- |
| `npm run dev` | Run client + server together with live reload |
| `npm run dev:server` | Run only the Express API on port `3001` |
| `npm run dev:client` | Run only the Vite dev server on port `5173` |
| `npm test` | Run all workspace test suites (Vitest) |
| `npm run typecheck` | Type-check every workspace |
| `npm run lint` | Lint the whole repo with ESLint |
| `npm run build` | Build the server and the client for production |
| `npm start` | Serve the production API and built UI on port `3001` |

## Environment variables

| Variable | Default | Where | Description |
| --- | --- | --- | --- |
| `PORT` | `3001` | server | HTTP listen port |
| `HOST` | `0.0.0.0` | server | HTTP bind address |
| `NOTES_DATA_FILE` | `server/data/notes.json` | server | JSON file used to persist notes |
| `VITE_API_TARGET` | `http://127.0.0.1:3001` | client (Vite) | Upstream API for the `/api` proxy |

The data directory is gitignored. A missing file is treated as an empty store
and created on the first successful write.

## Note shape

Every note is a JSON object:

| Field | Type | Rules |
| --- | --- | --- |
| `id` | string | UUID generated by the server |
| `title` | string | Required, trimmed, 1–200 characters |
| `body` | string | Optional, trimmed, at most 8,000 characters. Non-string bodies become `""` |
| `createdAt` | string | UTC ISO-8601 timestamp |
| `updatedAt` | string | UTC ISO-8601 timestamp. Differs from `createdAt` after an edit |

Notes are listed newest-`updatedAt` first.

## API (summary)

Full request/response details live in [`docs/api.md`](docs/api.md).

| Method | Path | Description |
| --- | --- | --- |
| `GET` | `/api/health` | Health check (`persist`: `ok`, `degraded`, or `unavailable`) |
| `GET` | `/api/notes` | List notes (newest first) |
| `GET` | `/api/notes/export` | Download all notes (`?format=json` or `md`) |
| `GET` | `/api/notes/:id` | Fetch a single note |
| `POST` | `/api/notes` | Create a note `{ title, body? }` |
| `PUT` | `/api/notes/:id` | Partially update a note `{ title?, body? }` |
| `DELETE` | `/api/notes/:id` | Delete a note |

Quick examples:

```bash
curl -s http://127.0.0.1:3001/api/health
curl -s -X POST http://127.0.0.1:3001/api/notes \
  -H 'Content-Type: application/json' \
  -d '{"title":"Hello","body":"First note"}'
curl -s http://127.0.0.1:3001/api/notes/export?format=md -o notes.md
```

Titles are limited to 200 characters and bodies to 8,000. Invalid JSON and
missing titles return `400`. `PUT` is a partial update: omitted fields keep
their current values. Unknown `/api` routes return JSON `404`. Disk write
failures return `503` and roll back in-memory state.

## Persistence

Notes are saved to `server/data/notes.json` (override with `NOTES_DATA_FILE`)
so they survive API restarts.

- Writes are atomic (`tmp` + rename).
- The API file watcher ignores `server/data`, so saving a note does not restart
  the server.
- A corrupt or unreadable file is never overwritten.
- Listing notes then returns `503` instead of an empty list
  (`persist: unavailable`).
- Invalid records (empty titles, over-length fields, bad dates, or duplicate
  ids) are skipped: health reports `degraded`, valid notes can still be listed,
  and writes return `503` until the file is repaired.
- Loaded timestamps are normalized to UTC ISO-8601 so mixed formats still sort
  and show “edited” correctly.

## User interface

- Language and text direction follow the browser: Arabic (`ar`, `ar-*`) uses
  RTL; everything else uses English + LTR.
- Title and body inputs use `dir="auto"` so mixed Arabic/Latin notes stay
  readable.
- Search matches titles and bodies after normalizing alef/tashkeel variants,
  hamza on waw/yeh, Persian yeh/kaf, Eastern Arabic and Persian digits, Latin
  accents, and extra spaces.
- Export buttons download the **currently matching** notes (the search filter),
  not necessarily the full store. The API export always dumps every note.
- Deleting a note asks for confirmation. Leaving a dirty form asks before
  discarding. Escape cancels an in-progress edit.
- If the API is unreachable on first load, the UI retries, then shows a
  **Try again** button.

## Project layout

```
.
├── client/                 # React + Vite frontend
│   └── src/
│       ├── App.tsx         # Notes UI (create, edit, search, export)
│       ├── api.ts          # REST client + response validation
│       ├── i18n.ts         # English/Arabic copy and search normalization
│       └── exportNotes.ts  # Client-side JSON/Markdown download
├── server/                 # Express + TypeScript API
│   └── src/
│       ├── index.ts        # Listen, persist path, optional static UI
│       ├── app.ts          # HTTP routes and error mapping
│       ├── notesStore.ts   # In-memory store + JSON file persistence
│       └── exportNotes.ts  # Server-side JSON/Markdown export
├── docs/                   # Extra documentation
├── eslint.config.js        # Shared ESLint flat config
└── package.json            # npm workspaces + top-level scripts
```

## Tests and CI

GitHub Actions (`.github/workflows/ci.yml`) runs typecheck, lint, tests, and
build on Node 20 and 22 for every pull request to `main`.

```bash
npm test           # client + server Vitest suites
npm run typecheck
npm run lint
```

## License

Private starter repository. No license file is published in this repo.
